task wrapper(type: Wrapper) {
  gradleVersion = '1.9'
}

allprojects {
  buildscript {
    repositories {
      mavenCentral()
    }
  }

  dependencies {
    repositories {
      mavenCentral()
    }
  }

  group = GROUP
  version = VERSION_NAME
}